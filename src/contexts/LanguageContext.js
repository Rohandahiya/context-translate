import React from "react";

const context = React.createContext(); // default value passed will be changed if we click another flag

//console.log(context);  // This context object contains provider and consumer properties

export default context;
